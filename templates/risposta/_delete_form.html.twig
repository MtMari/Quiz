<form method="post" action="{{ path('app_risposta_delete', {'id': rispostum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rispostum.id) }}">
    <button class="btn">Delete</button>
</form>
