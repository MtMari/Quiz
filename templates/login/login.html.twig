{% extends 'base.html.twig' %}

{% block title %}Log in{% endblock %}

{% block body %}

    {% import "macros/_navbar.html.twig" as navbar %}
    {{ navbar.navbar()}}

    <div class="d-flex justify-content-center">
        <form method="post">

            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            <div class="d-flex align-items-center flex-column card py-4 px-3 gap-2">

                <h1 class="mb-3 font-weight-normal">Log in</h1>

                {% if app.user %}
                    <div class="my-3">
                        <i class="bi bi-exclamation-lg text-danger"></i>
                        <span class="small-text">Login effettuato come: {{ app.user.userIdentifier }}.</span>
                        <a href="{{ path('app_logout') }}">Logout</a>
                    </div>
                {% endif %}

                <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">
                <div>
                    <label for="username">Username</label>
                    <input type="text" value="{{ last_username }}" name="_username" id="username" class="form-control" autocomplete="username" required autofocus>
                </div>
                <div>
                    <label for="password">Password</label>
                    <input type="password" name="_password" id="password" class="form-control" autocomplete="current-password" required>
                </div>
                <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">

                <button class="btn btn-success bg-gradient mt-3" type="submit">
                    Log in
                </button>
            </div>
        </form>
    </div>
    <div class="d-flex justify-content-center mt-2 small-text">
        <p>Oppure registrati <a href="{{ path('app_register')}}">qui</a></p>
    </div>
{% endblock %}
